const Events = () => {
  return (
    <section
      id="events"
      className="h-screen w-full p-24 flex justify-center items-center"
    >
      <span className="text-6xl">EVENTS</span>
    </section>
  );
};

export default Events;
